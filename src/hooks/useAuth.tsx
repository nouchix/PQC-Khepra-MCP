// Re-export everything from the split context files for backward compatibility
export { useAuth, useUserRoles } from '@/contexts/AuthContext';
export { AuthProvider } from '@/contexts/AuthProvider';
export type { AuthContextType } from '@/contexts/AuthContext';
